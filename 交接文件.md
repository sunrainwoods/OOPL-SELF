# 專案狀態交接文件 (Project Status Handoff)

## 專案概述
- **專案類型**：C++ 實作的類《吸血鬼倖存者》(Vampire Survivors) 遊戲。
- **底層引擎**：自製引擎「PTSD」(以 SDL2 / OpenGL 為基礎)。

## 近期完成實作功能

### 1. 武器系統框架
- 導入了 **Object Pooling (物件池)** 架構處理大量的子彈（飛刀、符文追蹤者），避免遊戲中頻繁產生與銷毀物件造成的效能浪費 (`reserve` 預先配置固定數量記憶體，以 `active` 旗標控制啟用的子彈)。

### 2. 飛刀武器 (Knife)
- **行為**：直線發射的投射物。
- **瞄準機制**：發射方向綁定 `m_PlayerLastMoveDir`（玩家最後移動的朝向）。
- **碰撞機制**：擊中敵人會造成傷害，並且子彈**沒有穿透效果**，擊中後即銷毀 (`active = false`)。

### 3. 符文追蹤者 (Runetracer)
- **行為**：發射後沿直線移動，並且在碰到螢幕邊緣時會發生**反彈**（以 `-velocity` 或特定軸向反轉實作）。
- **碰撞機制**：擁有**無限穿透**特性。為防止單次穿透判定過多，實作了基於時間的**無敵冷卻機制** (每個敵人對單一 Runetracer 有約 300ms 的冷卻判定)。
- **殘影特效 (Trail / History Positions)**：
  - **資料結構**：每一個 Runetracer 都帶有獨立的 `std::vector<glm::vec2> historyPositions` 來記錄過去數幀的座標。
  - **透明度技術限制與解法**：因 PTSD 底層 shader `Base.frag` 及 `Util::Image` 未提供動態改寫 Alpha/色調的 API，所以殘影的透明度改用**替換渲染圖片**的方式實作。
  - **目前渲染邏輯設定**：
    1. 殘影素材全部使用事先準備好的 `Runetracer65.png` (帶有 65% 透明度的圖)。
    2. 殘影有等比例縮放 (`trailScale`)：越舊的殘影尺寸越小。
    3. **繪製順序與 Z-Index 控制**：為解決舊殘影遮擋最新殘影及主體的顯示錯誤，迴圈採用**反向迴圈 (從最舊畫到最新)**，並在繪製過程**動態微調 `Z-Index`**（越舊殘影 Z 值越小如 4.4f，本體使用 4.5f）。這使得底層 OpenGL 的深度測試 (Depth Test) 能完美達成新圖覆蓋舊圖的效果。

### 4. 玩家機制修正與動畫升級
- **碰撞/受傷邏輯修正**：修正了原先玩家站著不動就不會扣血的 Bug。現在「受傷判定半徑」與「排斥推擠半徑」已經對齊一致 (`playerCollisionRadius + enemyCollisionRadius`)，確保怪物一貼上玩家防線就會正常扣血。
- **動態走路動畫**：在 `Player.hpp` 整合了底層 `Util::Animation`，載入 `Resources` 內的影格圖檔 (`left_main_walk1~3`, `right_main_walk1~3`)。並在 `SetState()` 新增 `isMoving` 判斷，輸入方向鍵時自動切換並播放走路動畫。

### 5. 特殊波次 (Special Wave) 與 Boss 突襲機制
- **物件池擴張**：為了支援大量生成，將敵人池預先配置 `m_MaxEnemies` 從 30 大幅擴展至 5000 隻，同時將寶石上限也提高至 2000 以應付殺敵盛況。
- **觸發條件**：遊戲局數達到 5 的倍數波次 (`m_CurrentWave % 5 == 0`) 時觸發。
- **機制表現**：
  - 在視窗外圍隨機處生成血多體胖的 Boss (具備 `isBoss = true` 屬性，會動態依移動方向切換左右素材圖)。
  - 於視野外圍產生完美的密集環狀陣型（利用圓形方程式排佈 30 隻特殊怪物 `Enemy4`），由外而內強勢包圍收縮，大幅增加壓迫感。

### 6. 極限效能最佳化：解決 O(N²) 與巨量運算卡頓
- 當場上湧入大量怪物時（超過 1000 隻以上），碰撞判定與推擠運算原先會造成單幀需要計算數百萬次浮點距離而引起嚴重卡頓，為此引入了以下三大核心技術優化：
  - **活耀清單 (Active Elements Pointer List)**：在 `Update` 初端立刻宣告 `std::vector<EnemyUnit*> activeEnemies` 只保留 active 旗標為 true 的可見怪物。將後續所有碰撞、傷害判定的 `for` 迴圈都改成這份指標清單，直接砍掉數以千計的無效輪詢。
  - **曼哈頓距離 (AABB Manhattan Fast Culling)**：在雙重迴圈中計算精準的 `glm::length`（平方根極度耗時）前，先用 `std::abs(dx) > radius || std::abs(dy) > radius` 過濾掉絕對碰不到的遠方碰撞體。並在所有戰鬥判定都改用「平方和」對決「半徑平方」方式，徹底根除耗時的 `sqrt` 呼叫。
  - **空間切割 Hash 網格 (Spatial Hashing Grid)**：實作了 150x150 像素的單元格地圖機制。我們把每個活躍得怪物用空間位址當作 Hash Key 映射進 `unordered_map`。當怪物互相推擠 (Separation) 尋找周圍同伴時，我們不再需要遍歷全場怪物，只需對周圍專屬的「九宮格 (3x3 grid)」搜查即可，將 $N^2$ 複雜度近乎切回 $O(N)$。

### 7. 遊戲節奏與數值平衡調整
- 為了使早期遊戲體驗更輕快（割草感），全面提升各武器的基礎攻擊力：
  - **近戰揮砍**：35.0 -> 70.0
  - **飛刀 (Knife)**：25.0 -> 50.0
  - **符文追蹤者 (Runetracer)**：15.0 -> 35.0

## 關鍵程式碼區域參考
- **`include/App.hpp`**：宣告了 `struct Knife` 與 `struct Runetracer`，配置了 Weapon 陣列、殘影結構 (maxHistory)。並加入了對應素材的 `std::shared_ptr<Util::Image>`。
- **`src/App.cpp`**：
  - `App::Start()`：載入各武器所需圖片。
  - `App::Update邏輯區`：包含隨機角度計算、發射計時器、飛刀/符文推移、邊界反彈，以及武器打擊敵人的 AABB 碰撞偵測 (含冷卻計算)。
  - `App::DrawGameObjects()`：執行實際畫面渲染與殘影圖片對應。

## 開發者備註
- 若日後需要加入其他透明度漸層，或其它具有長尾流特性的武器，可以直接沿用目前的 `historyPositions` 和「反向迴圈疊圖 + 素材置換」架構。
- 專案目前編譯 (`cmake --build`) 以及執行檔 `.exe` 皆運作正常順暢。
